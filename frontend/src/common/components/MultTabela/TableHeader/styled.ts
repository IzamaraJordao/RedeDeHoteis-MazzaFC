import styled from 'styled-components'

export const TableHeader = styled.div`
  flex-direction: column;
  align-items: center;
  position: static;

  div {
    margin-top: -30px;
  }

  button {
    color: #fff;
    border-radius: 8px;
   
  }
  input {
    width: 100px;
    border-radius: 6px;
    position: relative;
  }

  h4 {
    height: auto;
    width: 100%;
    margin-top: -22px;
    margin-bottom: 1px;
    position: static;
  }
`
